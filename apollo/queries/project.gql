query TodoByProject($projectId: ID!) {
    todoByProject(projectId: $projectId) {
        id
        title
        content
        status
        team {
            id
            name
            avatar
        }
        projectId
    }
}
